# Contributing

## Development
- Backend: `cd backend && pip install -r requirements.txt && uvicorn receiptrip.main:app --reload`
- Web: `cd web && npm install && npm run dev`

## Standards
- Privacy-first defaults only.
- Keep OCR/AI local by default.
- Add tests for backend endpoints.
