# Security Policy

ReceiptRipper handles sensitive receipt data.

- Default deployment is local-first and self-hosted.
- Receipt binary files are encrypted at rest using an app secret.
- Third-party OCR/LLM services are disabled by default.
- Optional Ollama integration must run on localhost.

To report vulnerabilities, open a private security advisory in GitHub.
